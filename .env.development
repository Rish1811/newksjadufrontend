# Local development.
# Empty = use a relative /api path, which vite.config.js proxies to the backend.
# This is what makes the site work from other devices on the same Wi-Fi.
VITE_API_BASE=

# Where the Vite dev-server proxy forwards /api and /uploads.
VITE_PROXY_TARGET=http://localhost:5000
